telegram:
  token: "PASS HERE YOUR BOT TOKEN"
  # Send /chatid with bot enabled to receive chatID
  chat_id: 0
  # Send /topicmessage with bot enabled to receive topicID
  topic_id: 0
chat:
  # Display join messages
  join: true
  # Display leave messages
  leave: true
  # Display death messages
  death: true