main: '${main}'
prefix: '${prefix}'
description: '${description}'
name: '${name}'
version: '${version}'
authors: [ "${authors}" ]
author: "${author}"
website: "${url}"
api-version: 1.18
softdepend: [ "luckperms" ]
depend: [ ]
loadbefore: [ ]
libraries: ${libraries}
commands:
  mbreload:
  link: